# IIP-01 kaynak ve davranış envanteri

Tarih: 2026-09-19
Kart: IIP-01
Kaynak HEAD: `e794e7bcc637b2431dcfae29f645ef1b02a2b51c`
Kapsam: yalnız mevcut Şeyma kodunun salt-okur envanteri; üretim ve test dosyası değiştirilmedi.

## Giriş zinciri

`Bugün` dış uygulama sekmesi `app/core/render.js:505-557` içindeki marka ve alt gezinme düğmelerinden `App.go(id,event)` (`app.js:3057`) yoluna girer. `saygi` sekmesi `saygiHTML()` ile `app/core/saygi.js:241` içindeki İlham & İbadet hub'ını üretir. Hub'ın beş iç sekmesi `faithNavHTML()` (`app/core/saygi.js:240`) tarafından `Öz`, `Öncü`, `İman`, `Zikir`, `Rapor` olarak üretilir; `Zikir` özelliği kapalıysa kaynak dört sekmeye düşer. `App.setFaithTab(tab)` (`app.js:3979`) yalnız `ui.faithTab` değerini değiştirip render eder.

| Giriş | Entrypoint → handler → üretim gövdesi | Data / state sahibi | DOM veya panel yüzeyi | Fixture / doğrulama satırı |
|---|---|---|---|---|
| Bugün | `appHeaderHTML/navHTML` → `App.go('bugun')` → `bugunHTML()` (`app/core/render.js:505-557,660`, `app.js:3057,5417`) | `ui.tab`; bugünün `data.days[date]`; günlük kartların kendi alanları; reminder inbox girdisi | `#app` günlük görünüm; `bugunHTML()` | `tests/app/test_app_surface_daily_boundary.js`, `tests/app/test_today_card_preferences.js`, `node .claude/skills/run-seyma/zikr-harness.mjs` |
| Öz | `App.go('saygi')` → `saygiHTML()` → `saygiPreviewHubHTML()` (`app/core/saygi.js:239-241`) | `ui.faithTab='oz'`; günün öncüsü, prayer özeti, zikir önizlemesi, Kur'an hub kartı | `.saygi-page`, `.faith-v2-nav`, `.saygi-preview-hub` | `tests/app/test_saygi_boundary.js`; Zikirmatik harness: “Beşli hub sekmesi render” |
| Öncü | `App.setFaithTab('oncu')` → `saygiPreviewCardHTML()` + `saygiCollectionCardHTML()`; detayda `App.openSaygiPreview()` / `App.openSaygiCollectionPerson()` (`app/core/saygi.js:177,239-243`, `app.js:3548-3579`) | `data.saygi.collection`, bugünün `data.days[date].reading.entries`; `SaygiPeople`; `ui.saygi*` | `#saygi-preview-card`, `.sg-collect`, Öncü modalı `#sey-ov-card` | `tests/app/test_saygi_boundary.js:128-175`; harness: koleksiyon, modal ve Okudum akışı |
| İman | `App.setFaithTab('iman')` → `faithCornerCardHTML()`; açılış `App.openFaithCorner()` (`app/core/saygi.js:176,239`, `app.js:4037`) | `data.days[date].prayer`, `data.settings.prayer`, `ui.faithOpen`; `SeymaPrayer` hesap/cache kayıtları | `#faith-preview-card`; `faithCornerOverlayHTML()` içinde `#sey-ov-back/#sey-ov-card` (`app/core/saygi.js:184-211`) | `tests/app/test_prayer_boundary.js`, `tests/app/test_saygi_boundary.js:160-175`; harness: İman kartı/overlay |
| Zikir | `App.setFaithTab('zikir')` → `zikrPreviewCardHTML()`; tam ekran `App.openZikr()` (`app/core/zikir.js:614`, `app.js:3917`) | `data.zikr`; bugünün `data.days[date].zikr`; `ui.zikr*`; `SeymaZikr` registry | `#zikr-preview-card`; `zikroverlayHTML()` içindeki `#zikr-overlay/#zikr-screen`, `#zikr-tabs` (`app/core/render.js:1406-1424`) | `tests/app/test_zikir_boundary.js`, `test_zikir_view_boundary.js`, `tests/app/test_zikr_manual_entry.js`; harness: 95/95 |
| Rapor | `App.setFaithTab('rapor')` → `faithRaporCardHTML()` + `faithAnnualHeatmapHTML()` (`app/core/saygi.js:214-215,239`) | `data.days[date].prayer`, `data.days[date].zikr`; `ui.faithHeatYear`; `faithWeekKPIs()` | `.sg-faith-hero`, `.sg-faith-year`; gün hücresi `App.openFaithHeatDay()` ve `App.faithHeatYear()` (`app.js:3980-3984`) | `tests/app/test_report_boundary.js:117-120`, `tests/app/test_saygi_boundary.js`; harness: haftalık rapor + yıllık ısı haritası |
| Kıble overlay | `qiblaHubCardHTML()` → `App.openQibla()` → `qiblaOverlayHTML()` (`app/core/saygi.js:216-217`, `app.js:6047`) | `data.settings.prayer.location`; `ui.qiblaHeading`, `qiblaListening`, sensor error; `qiblaMetrics()` | `#qibla-overlay/#qibla-dialog`, `#qibla-live-needle`, `#qibla-live-status` (`app/core/saygi.js:221-234`) | `tests/app/test_saygi_boundary.js:152-175`, `tests/app/test_prayer_boundary.js`; harness: azimut, sensör ve hedefli DOM güncellemesi |
| Öncü/okuma overlay | `App.openSaygiPreview()` / `App.openSaygiCollectionPerson()` → `saygiPersonModalHTML()` (`app.js:3548-3579`, `app/core/saygi.js:243`) | `SaygiPeople`; `ui.saygiPersonOpen`, `saygiArticle`, `saygiReadReady`; okuma kaydı `data.days[date].reading.entries` | `#sey-ov-card`, `#sey-ov-body`, `saygi-read-button[-modal]`; kaynak ve hata/yeniden dene yüzeyi | `tests/app/test_saygi_boundary.js:141-175`; harness: kaynak yükleniyor/hata/Okudum/önceki-sonraki yolu |
| Kur'an overlay | `quranJourneyHubCardHTML()` → `App.openQuranJourney()` / `App.openQuranSurah()` (`app/core/render.js:1427-1473`, `app.js:5725,5949-6005`) | `data.quranJourney`; `ui.quranJourney*`; frozen sûre/âyet/transport content | `#quran-screen`, `#quran-scroll`, `#quran-row-*`; video, not ve durum kartları | `tests/app/test_quran_boundary.js`, `tests/quran/test_quran_*.js`, `tests/panel/test_faz11_panel.js` |
| Reminder inbox | `reminderInboxCardHTML()` → `App.reminderInboxPrimary/Overflow()` → `App.openReminderTarget()` (`app/core/reminders.js:3378-3414`, `app/core/reminderSurface.js:541-557`) | `data.reminders`; bugünün `data.days`; local delivery/action/permission journal; `data.eventLog` | `#sey-reminder-inbox-card`, primary/overflow actions, `#sey-reminder-inbox-center` | `tests/reminders/run-reminder-smoke.mjs`, `test_reminder_ui_boundary.js`, `test_reminder_app_acceptance.js`, `test_reminder_app_privacy.js` |
| Reminder center, native/deep-link | `App.openReminderCenter()` / `App.handleReminderNativeClick()` / `App.openReminderTarget()` (`app.js:3229-3235`, `app/core/reminderSurface.js:511-557`) → `reminderCenterOverlayHTML()` (`app/core/reminders.js:318`) | `data.reminders` tercihleri; `localStorage:seyma-reminder-*` delivery/action/permission kayıtları; sync'e yalnız güvenli özet | `#sey-ov-card` reminder center; hedefe göre İman, Zikir, Öncü, okuma, günlük, sağlık veya ayarlar | `tests/reminders/test_reminder_notification_boundary.js`, `test_reminder_cross_surface_*.js`, `test_reminder_panel_*.js`, `tests/panel/test_panel_p1_projection.js` |

## Panel sınırı

Şeyma uygulama yüzeyi ile ÆON observer paneli aynı handler veya DOM ağacını paylaşmaz. Observer panel için tek güvenli bağ `panel/panelCoverageManifest.js:1-3,118-132` içindeki redacted projection sözleşmesidir. Bu nedenle tabloda “panel” karşılığı olmayan uygulama yüzeyleri açıkça bağımsız bırakıldı; reminder için current-panel coverage ve lineage ayrıca mevcut. Panel fixture'ı uygulama handler'ının çalıştığını kanıtlamaz; yalnız projection/redaction ve observer durumunu kanıtlar.

## Değişmezler

- `index.html:101-138` bütün frozen content/core registry'lerini cache-bust ile `app.js` ve `sync.js` öncesinde yükler; `quran.js` → `saygi.js` → `app.js` sırası fixture ile korunur.
- `app.js:299-322` `SeymaSaygi` ve `SeymaRender` shim'lerini, `app.js:3057`, `3979`, `4037`, `6047` shell handler'larını taşır. Gövdeleri ve imzaları IIP-01 kapsamında değiştirilmedi.
- `app.js` mevcut `data`/`ui` sahipliğini korur. IIP-01 envanteri veri modelini yeniden yorumlamaz; özellikle raporun mevcut `% uyum` kopyası sonraki anlamsal kartın açık inceleme konusudur.
- `tools/shell-inventory.mjs --gate` canlı ölçümü: 7.610 toplam satır, 7.800 bütçe altında; 554 App handler, 0 Legacy, 57 HTML-builder satırı ve 408 reminder fonksiyon/kod satırı.
