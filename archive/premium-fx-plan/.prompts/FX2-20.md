# FX2-20 — Güneş Saati: Gerçek `sunrise`/`sunset`

| | |
|---|---|
| **Dalga** | 5 — Canlı Zemin |
| **Tip** | Entegrasyon + CSS |
| **Hedef** | 4 zaman sahnesi canlı; zemin gerçekten değişir |
| **Ön koşul** | FX2-19 tamam |
| **Sonraki** | FX2-21 |

## NEDEN
Mevcut `classForHour` **sabit** saat aralıkları kullanıyor (5–8 / 9–16 /
17–20 / diğer). Ocak'ta saat 17:00 gecedir, Haziran'da gündüz — sabit aralık
ikisini de yanlış yapar.

`data.weather.spots[0].sunrise` / `.sunset` **zaten kayıtlı**. Gerçek güneş
saatiyle çalışan bir zemin, "premium" hissin sessiz ama en ikna edici
parçasıdır.

Ayrıca mevcut `theme-time-*` **yalnız iki elemanın `box-shadow` rengini**
değiştiriyor. Bu kart zemini gerçekten değiştirir.

## 1. DEĞİŞTİRİLECEK
`app/core/timeTheme.js` · `app/styles.css` · `app.js` (**tek** guard satırı) ·
`index.html`

## 2. DOKUNULMAYACAK
`classForHour` (fallback olarak korunur, imzası değişmez) · `SeyTimeTheme.apply`

## 3. ADIMLAR

### Adım 1 — Güneş saati hesabı (`timeTheme.js`, `SeyAmbience` içine)

```js
    // Gerçek güneş saatine göre 4 dilim. sunrise/sunset yoksa
    // SeyTimeTheme.classForHour()'a düşer (asla kırılmaz).
    timeClass: function(now, spot){
      var t = now || new Date();
      var s = (spot !== undefined) ? spot : wx();
      var sr = s && s.sunrise ? new Date(s.sunrise) : null;
      var ss = s && s.sunset  ? new Date(s.sunset)  : null;
      if (!sr || !ss || isNaN(sr.getTime()) || isNaN(ss.getTime())){
        // FALLBACK — mevcut sabit aralıklar
        var c = window.SeyTimeTheme.classForHour(t.getHours());
        return c.replace('theme-time-', 'amb-time-');
      }
      var m = 60000;
      if (t >= new Date(sr.getTime() - 45*m) && t < new Date(sr.getTime() + 75*m)) return 'amb-time-dawn';
      if (t >= new Date(sr.getTime() + 75*m) && t < new Date(ss.getTime() - 90*m)) return 'amb-time-day';
      if (t >= new Date(ss.getTime() - 90*m) && t < new Date(ss.getTime() + 45*m)) return 'amb-time-dusk';
      return 'amb-time-night';
    },
```

`scene()` içindeki `time:` alanını `this.timeClass(now, spot)` yap.

### Adım 2 — `apply()` gerçek gövdesi

```js
    apply: function(now){
      var root = document.getElementById('root');
      if (!root) return false;
      var s = (window.SeymaState && window.SeymaState.data && window.SeymaState.data.settings) || {};
      // Premium kapalı → tüm amb-* sınıfları temizlenir, katman söner.
      var all = root.className.split(/\s+/).filter(function(c){ return c.indexOf('amb-') === 0; });
      all.forEach(function(c){ root.classList.remove(c); });
      if (!s.premiumAtmosphere) return false;
      var sc = window.SeyAmbience.scene(now);
      root.classList.add(sc.time);
      if (sc.weather) root.classList.add(sc.weather);
      if (sc.season)  root.classList.add(sc.season);
      root.style.setProperty('--wx-intensity', String(sc.intensity.toFixed(2)));
      root.style.setProperty('--amb-seed', String(sc.seed.toFixed(3)));
      root.style.setProperty('--wx-dim', sc.isDay ? '1' : '0.72');
      return true;
    },
```

### Adım 3 — CSS: 4 zaman sahnesi × 2 tema

`app/styles.css` FX bölümüne. **Zemin gradienti buradan gelir.**

```css
/* FX2-20: canlı zemin — zaman katmanı. --page'i sahne ezer. */
#root[class*="amb-time-"]{ transition: background var(--dur-5) var(--ease-glide); }

#root.amb-time-dawn { --page: linear-gradient(170deg,#FFF6EC 0%,#FBEBD8 46%,#F4EFF2 100%); }
#root.amb-time-day  { --page: linear-gradient(170deg,#FDFBF7 0%,#F7F1E8 46%,#F2EFF4 100%); }
#root.amb-time-dusk { --page: linear-gradient(170deg,#FBF0E4 0%,#F6E4D6 46%,#EFE9F0 100%); }
#root.amb-time-night{ --page: linear-gradient(170deg,#F4F2F6 0%,#EDEBF2 46%,#E9E9F0 100%); }

#root[data-theme="dark"].amb-time-dawn { --page: linear-gradient(170deg,#0B0806 0%,#0A0705 100%); }
#root[data-theme="dark"].amb-time-day  { --page:#000000; }
#root[data-theme="dark"].amb-time-dusk { --page: linear-gradient(170deg,#0A0706 0%,#080607 100%); }
#root[data-theme="dark"].amb-time-night{ --page: linear-gradient(170deg,#04060A 0%,#000000 100%); }

@media (prefers-reduced-motion: reduce){
  #root[class*="amb-time-"]{ transition:none!important; }
}
```

> **Ton kayması ÇOK İNCE tutulur.** Uygulamanın sakin kimliği bozulmamalı;
> amaç "fark ettirmeden ferahlatmak", "dikkat çekmek" değil.

### Adım 4 — `app.js` bağlantısı (TEK satır)
`paint()` içindeki mevcut `SeyTimeTheme.apply()` guard'ının **hemen altına**:
```js
try{ if(window.SeyAmbience && typeof window.SeyAmbience.apply==='function') window.SeyAmbience.apply(); }catch(e){}
```
> `SeyTimeTheme.apply()` çağrısı **silinmez** — `theme-aurora` sınıfını o
> yönetiyor (FX2-25 ona bağlı).
> `render()` 30 sn'lik foreground poll'da da çalıştığı için sahne
> kendiliğinden tazelenir. **`setInterval` EKLEME.**

### Adım 5 — Cache bump (`timeTheme.js`, `styles.css`, `app.js`)

## 4. DOĞRULAMA
```bash
node --check app/core/timeTheme.js && node --check app.js
grep -c 'timeClass'      app/core/timeTheme.js    # >= 2
grep -c 'classForHour'   app/core/timeTheme.js    # >= 2 (fallback korundu)
grep -c 'amb-time-'      app/styles.css           # >= 8
grep -c 'SeyAmbience'    app.js                   # 1 (tek guard satırı)
grep -c 'setInterval'    app/core/timeTheme.js    # 0
node .claude/skills/run-seyma/driver.mjs 2>&1 | grep -c '^FAIL'   # 0
node tests/app/test_premium_time_theme.js                          # 53/53
node tests/app/test_fx2_palette_contrast.js                        # kontrast korundu
for f in tests/app/*.js; do node "$f" >/dev/null 2>&1 || echo "FAIL $f"; done
node tools/fx-coverage.mjs                        # M12 >= 4
```

**Fallback testi (sunrise/sunset yokken kırılmamalı):**
```bash
node -e "
global.window={SeymaState:{data:{weather:null,settings:{premiumAtmosphere:true}}}};
global.document={getElementById:()=>null};
require('/Users/m_ras/Desktop/seyma/app/core/timeTheme.js');
console.log('veri yokken:', window.SeyAmbience.timeClass(new Date('2026-01-15T17:00:00')));
console.log('veri varken:', window.SeyAmbience.timeClass(
  new Date('2026-01-15T17:00:00'),
  {sunrise:'2026-01-15T08:10:00', sunset:'2026-01-15T17:20:00', code:0, isDay:true}));
"
```
Beklenen: ilki `amb-time-dusk` (sabit aralık 17–20), ikincisi `amb-time-dusk`
(gerçek gün batımı 17:20 → −90dk = 15:50'den itibaren dusk). **İkisi de
çalışmalı, hiçbiri `undefined` olmamalı.**

## 5. BİTTİ SAYILIR
- [ ] **M12 ≥ 4**
- [ ] `sunrise`/`sunset` varken gerçek hesap, yokken `classForHour` fallback
- [ ] Fallback yolu **hiç kırılmıyor** (veri yokken de sınıf dönüyor)
- [ ] `--page` 4 dilim × 2 temada tanımlı, geçiş `--dur-5` crossfade
- [ ] `premiumAtmosphere:false` → tüm `amb-*` sınıfları **temizleniyor**
- [ ] `setInterval` eklenmedi
- [ ] Kontrast fixture'ı hâlâ 8/8

## 6. GERİ ALMA
Git ile geri al: `app/core/timeTheme.js`, `app/styles.css`, `app.js`, `index.html`

## 7. KAYIT
`FX2-STATE.json`: `lastCompletedPrompt:"FX2-20"`, `nextPrompt:"FX2-21"`
Commit: `fx2: FX2-20 güneş saatine göre canlı zemin`
