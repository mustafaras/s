# FX2-27 — Tam Regresyon ve Kapsam Raporu

| | |
|---|---|
| **Dalga** | 7 — Kapanış |
| **Tip** | Salt-okur QA |
| **Hedef** | 0 FAIL + kapsam kanıtı |
| **Ön koşul** | FX2-26 tamam |
| **Sonraki** | FX2-28 |

## KURAL
**Hiçbir kaynak dosya değiştirilmez.** Yalnız
`premium-fx-plan/deliverables/FX2-KAPSAM-RAPORU.md` üretilir.

## 3. ADIMLAR

### Adım 1 — Syntax
```bash
for f in app.js sync.js sw.js panel/panel.js panel/panelCoverageManifest.js \
         app/core/state.js app/core/mediaFx.js app/core/timeTheme.js \
         app/core/syncGlue.js app/core/helpers.js tools/fx-coverage.mjs; do
  node --check "$f" >/dev/null 2>&1 && echo "OK   $f" || echo "FAIL $f"; done
```

### Adım 2 — Harness
```bash
node .claude/skills/run-seyma/driver.mjs 2>&1 | tail -3
node .claude/skills/run-seyma/zikr-harness.mjs 2>&1 | tail -1        # 95/95
node .claude/skills/run-seyma/verify-state-migration-boundary.mjs
node .claude/skills/run-seyma/verify-state-helper-boundary.mjs
node .claude/skills/run-seyma/verify-state-adapter-contract.mjs
```

### Adım 3 — Tüm fixture aileleri
```bash
p=0; f=0
for t in tests/app/*.js tests/panel/*.js tests/panel-v2/test_panel_v2_*.js tests/quran/*.js; do
  [ -e "$t" ] || continue
  if node "$t" >/dev/null 2>&1; then p=$((p+1)); else f=$((f+1)); echo "  FAIL $t"; fi
done
echo "$p PASS / $f FAIL"
node tests/reminders/run-reminder-smoke.mjs 2>&1 | tail -2
```

### Adım 4 — Kapsam kapısı
```bash
node tools/fx-coverage.mjs --gate && echo "GATE PASS"
node tools/fx-coverage.mjs --json > "$TMPDIR/fx2-final.json"
```

### Adım 5 — Değişmezler (I1–I8)
```bash
grep -oE 'App\.[A-Za-z0-9_]+\s*=[^=]' app.js | grep -oE 'App\.[A-Za-z0-9_]+' | sort -u | wc -l   # 718
grep -o 'onclick=' app.js | wc -l                     # 391
grep -c '<script src="app.js' index.html              # 1
grep -c 'preventDefault' app/core/mediaFx.js          # 0
grep -c 'setInterval'    app/core/timeTheme.js        # 0
grep -c 'fetch('         app/core/timeTheme.js        # 0
grep -c 'fetch('         app/core/mediaFx.js          # 1 (yalnız bulut TTS)
git diff --stat main -- sync.js                       # BOŞ olmalı
```

### Adım 6 — Rapor yaz
`deliverables/FX2-KAPSAM-RAPORU.md`:

1. **Öncesi/sonrası kapsam tablosu** (M1–M13: taban → şimdi → eşik → ✅/❌)
   — *raporun kalbi budur; FX-1 kapanışında olmayan şey.*
2. Dalga bazında ne uygulandı (28 kart)
3. Fixture envanteri ve sonuçları
4. Değişmezlik kanıtları (yukarıdaki komut çıktıları)
5. **Kanıt seviyesi ayrımı:**
   - **K1 kaynak/test:** ✅ (bu rapor)
   - **K2 yerel görsel:** varsa
   - **K3 cihaz kabulü:** ⏳ **kullanıcıdan bekleniyor**
6. **Bilinen sınırlar (dürüstçe):**
   - iOS'ta `navigator.vibrate` yok — haptik bonus kanal
   - iOS sessiz anahtarı WebAudio'yu susturabilir
   - Bulut TTS `openaiKey` gerektirir (yerel fallback açık)
   - `render()` hâlâ tam `innerHTML` — sürekli hareket sınırlı
   - Canlı zemin `data.weather` boşken zaman+mevsim ile çalışır (hava yok)

## 5. BİTTİ SAYILIR
- [ ] Tüm aileler **0 FAIL**
- [ ] `--gate` **PASS** (13 metriğin hepsi eşikte)
- [ ] `App.*` = 718, `onclick` = 391
- [ ] `sync.js` diff'i **boş**
- [ ] Rapor K1/K2/K3 ayrımını açıkça yapıyor
- [ ] **Hiçbir kaynak dosya değişmedi** (`git status --short` yalnız
      `deliverables/`)

## 6. GERİ ALMA
Üretilen raporu sil: `premium-fx-plan/deliverables/FX2-KAPSAM-RAPORU.md`

## 7. KAYIT
`FX2-STATE.json`: `lastCompletedPrompt:"FX2-27"`, `nextPrompt:"FX2-28"`
Commit: `fx2: FX2-27 tam regresyon ve kapsam raporu`
