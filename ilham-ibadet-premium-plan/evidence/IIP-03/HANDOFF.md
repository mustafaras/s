# IIP-03 devir

IIP-03 tamamlandı: `fajr`/`sunrise` tarihsel ayrımı ve mevcut payda davranışı kaynak/testten belgelendi; belirsiz oran kopyası `kayıtlı vakit payı` / `kayıtlı vakit` olarak güvenli betimleyici dile çekildi. Hesap, alan, mapping, veri yazımı ve call graph değiştirilmedi.

Açık devir: `dayCount*6` ve `days*6` paydasının boş/migration/unknown/intentional-zero ayrımı IIP-14/15'te çözülmeli. Bu kart başarı, ibadet uyumu veya gerçek kullanıcı niyeti iddiası taşımaz.

Son doğrulama: source syntax, IIP-03 synthetic contract, Saygı boundary ve prayer boundary PASS. Commit/push/merge/deploy yapılmadı; device/release acceptance yok.
