# KAO — Olay günlüğü

| Seq | Tarih | Olay | Kanıt |
|---|---|---|---|
| 1 | 2026-09-20 | Program açıldı: repo incelemesi (quran.js/zikir.js/saygi.js/state.js/styles.css/panel manifest/IIP planı) + web araştırması; 9 belge + STATE yazıldı. Üretim kodu değişmedi. | `kuran-ogreniyorum/01…09`, `KAO-STATE.json`; `git status` yalnız yeni klasör |
| 2 | 2026-09-20 | v2: kullanıcı kararları (IIP'den bağımsız; tek hub kartı; sıfırdan öğrenen; telaffuz çekirdek). Ek araştırma (Türk öğrenci telaffuz hataları, HVPT meta-analizleri, hareke/Abu-Rabia, %95–98 kapsam, ILH, ses veri setleri). Yeni: 10-TELAFFUZ, 11-BAGIMSIZLIK; 02/03/04/05/07/09/README güncellendi; 28 kart; D-08..D-10. Üretim kodu değişmedi. | `git status`: yalnız `kuran-ogreniyorum/` |
| 3 | 2026-09-20 | v3: A/B/C önerileri kullanıcı kararıyla **bağlayıcı gereksinim** oldu → 12-EK-GEREKSINIMLER (26 madde, kart + kabul kontrolü); 03/04/05/07/08/README/STATE güncellendi; 2 yeni kart (KAO-28b, KAO-16b), 30 kart. Üretim kodu değişmedi. | `git status`: yalnız `kuran-ogreniyorum/` |
| 4 | 2026-09-20 | Tutarlılık denetimi: 30 kart (07 = STATE), 26 R-id (12 = STATE), 11 ekran, 13 fixture adı, tüm iç bağlantılar ve 23 repo sembolü doğrulandı; `SeyAudio.say` → gerçek API `SeyAudio.voice/speak` düzeltildi; TTS politikası 02/05/10'da hizalandı; R-A1 `settings.targetBed`/`caffeineTargetBed()`'e bağlandı; sessiz mod kapı kuralı 03'e eklendi; sayaçlar (ekran/handler) güncellendi. | Denetim betiği çıktısı: issues 0, table issues 0, presentation forms 0 |
| 5 | 2026-09-20 | Uygulama altyapısı: `UYGULAMA-PROMPTLARI.md` (37 sıralı prompt: P00 + 30 kart + D1–D6 dalga denetimleri; ortak sözleşme S0–S10, K1–K10 değişmezleri; izinli dosya/kontrol listeleri STATE'ten üretildi), `KAO-STATE.json` v4 (`cards{}` dosya/kontrol/req/deps, `promptOrder`, `bootstrap`, `auditScope`), `.anti-amnesia/CURRENT-STATE.md` (üretilmiş), `tools/kao-plan-check.mjs` (12/12 self-test; commit kapsamı, sıra, kapı, ledger, yasak ifade, 4 liste), `templates/EVIDENCE|HANDOFF`. Ledger `tracking/` → `.anti-amnesia/`. Üretim kodu değişmedi. | `node kuran-ogreniyorum/tools/kao-plan-check.mjs --render` PASS (0 warn) |
| 6 | 2026-09-20 | Başlangıç öncesi kapsamlı kontrol: 10 repo dosya/satır atfı ve 3 yükleme listesi doğrulandı; prompt bölüm atıfları (01–12 §x) eksiksiz; prompt ↔ STATE dosya/kontrol/req listeleri birebir (0 fark); denetleyici yaşam döngüsü simülasyonu 21/21 (başlat/bitir/sıra atlama/kapı/waiting_user/deps/kapsam/yasak ifade/4 liste). Bulunup düzeltilen: (1) modül import edilince CLI çalışıp exit ediyordu → IS_MAIN koruması; (2) `tests/kao/repo-root.js` gereksizdi → ortak `tests/repo-root.js`; (3) sessiz saat API'si `SeyAudio.isQuietTime()` olarak düzeltildi; (4) `chore(kao)` commit'leri de kapsam denetimine alındı (self-test 13/13). Üretim kodu değişmedi. | `kao-plan-check --render` PASS (0 warn); lifecycle sim ALL PASS |
| 7 | 2026-09-23 | KAO-P00 başladı: kullanıcı ilk prompt için uygulama yetkisi verdi; yerel `kuran-ogreniyorum` branch'i seçildi, kapsam bootstrap dosyalarıyla sınırlandı. | `activePrompt=KAO-P00`; üretim/push/merge/tag/deploy yetkisi yok |
| 8 | 2026-09-23 | KAO-P00 tamamlandı: `tests/kao/README.md` içinde 13 fixture planlandı; ham korpus girdileri `content/inputs/` ile Git dışında bırakıldı; üretim dosyası değişmedi. | `kao-plan-check` PASS (0 warn); self-test 13/13; Kur'an kataloğu 70/70; `git diff --check` PASS; `evidence/KAO-P00/EVIDENCE.json` |
