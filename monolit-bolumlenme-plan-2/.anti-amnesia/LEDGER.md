# MON2 · LEDGER (yalnız-eklemeli)

| seq | tarih | kart | önce (app.js / Legacy / reminder gövde / HTML) | sonra | kapı | sapma / not |
|---:|---|---|---|---|---|---|
| 0 | 2026-09-14 | plan | 13.139 / 22 / 3.247 / 1.129 | — | `shell-inventory --gate` PASS (13.200 bütçesi) | Plan yazıldı; `tools/shell-inventory.mjs` eklendi; kod taşınmadı. Teşhis: MON serisi satır hedefi koymadı; 1-satır shim standardı + reminder Legacy çift gövdesi + fixture'ların `reminders.js`'siz boot etmesi MON-40..43'ü 360 satırla sınırladı. |
| 1 | 2026-09-14 | MON2-01 | 13.139 / 22 / 3.247 / 1.129 | 13.150 / 22 / 3.247 / 1.129 | syntax; `--gate` PASS; driver; zikr; verify-state×3; tests/app 50/52; quran 9/9; panel 23/23; reminder smoke 20/20 | **Yapılan:** reminderSurface.js iskeleti (Object.freeze — acceptance RUNTIME_MODULES sözleşmesi); 4 liste + index; app.js bag (data/ui/app/save/render, throw); K6: 14 fixture + acceptance `APP_SHELL_REGISTRIES`; K7; K8. **Sapmalar:** (1) kart "11 fixture" dedi → gerçek: +migration/concurrency/integrated_ux/aeon_message_expand (tam boot), −premium_fx_utils/end_to_end_lineage (yalnız metin/slice); (2) `reminders.js` cache-bust bump edilmedi (içerik değişmedi, konvansiyon #5); (3) 4 app_surface fixture'ı `app.js?v=` literalini pinliyor → 20260914b; (4) app.js delta +11 (kabul ≤+10; 8'i kod); (5) acceptance `modules:` override'ları kabuk registry'lerini de dışlıyordu — K1 sonrası kırılacaktı, ileriye dönük düzeltildi. **Kapsam dışı, önceden var:** test_premium_settings 38/39, test_premium_voice 59/67 (temiz HEAD worktree'de birebir aynı). |
| 2 | 2026-09-14 | (MON2 dışı) | — | — | premium ailesi 8/8 PASS; voice 68/68, settings 39/39; `_pinnedHour=3` ile de deterministik | **Seq 1 düzeltmesi:** "önceden var, kırık" tanısı yanlıştı. `test_premium_settings` 38/39 ve `test_premium_voice` 59/67, koşu saati 06:30 quiet-time (23–07) penceresinde olduğu için düştü — `mediaFx.js:115` `isQuietTime()` gerçek `new Date().getHours()` okur, `voice()` false döner. Temiz HEAD worktree'de aynı sonucun sebebi de aynı dakikalardı; 09:36'da ikisi de yeşildi. Düzeltme: iki fixture'da `Date.prototype.getHours` sabit 12; voice Test 5'teki tautolojik `r===true||r===false` assert'i gerçek-saat yolunu 12→speak / 23→sessiz olarak sınar. Kod (mediaFx) değişmedi. |
| 3 | 2026-09-14 | MON2-02 | 13.150 / 22 / 3.247 / 1.129 | 10.277 / 0 / 947 / 928 | syntax (app+sync+core×24); smoke **21/21**; `--gate` PASS (11.300 bütçesi, 10.277/0/947/928); driver+zikr 95/95; verify-state B1/B2/B3; tests/app **53/53**; App.x=554; onclick=391; dump bugun/ayarlar bayt-eşit | **Yapılan:** REMINDER_* sabitleri + saf/B1 gövdeler + görünüm üreticileri → reminders.js (360→4.189 satır, registry +171); 22 `reminder*Legacy` silindi; deps bag 33→51 getter üye; view bag app.js'te (sections yok); K4/K5 reminderSurface iskeleti. **Sapmalar:** (1) scope getter deps-only — viewDeps önceliği sistemik hataydı, cold-sandbox `ui` çökmesi bunu açığa çıkardı; (2) modül yükleme sırası boot sabitlerinden önce geldiği için `REMINDER_PRAYER_KEYS` (6 tüketici) ve `REMINDER_PRIVACY_SCHEMAS` lazy builder'a alındı (`reminderPrivacySchemasObject()`), export satırı raw adı korur; (3) `migrateReminderState` + `REMINDER_*` boot sabitleri app.js'te kaldı (I1-I6 migrate/rebind sahipliği); (4) localStorage guard deps-bag'e bağlandı; (5) `ui.reminderPreviewLegacyId` korundu; (6) `deliveryRuntimeLoaded` sınır alanı eklendi; (7) `reminderEnginePure*` gövdeleri + wrapper rewiring; (8) fixture devirleri — K8 ilkesi (pin gövdeyi izler): `cross_surface_status` extractFunction girinti+yorum duyarlı süslü tarama olarak yeniden yazıldı (Türkçe yorum apostrofları tırnak-sanıyordu) ve birleşik kaynak sırası [reminders, reminderSurface, app] oldu (app.js shim önce bulunuordu → "SEYMA_REMINDERS is not defined"); `integrated_ux` `sey-reminder-inbox-live` APP_REMINDER_SOURCE'a alındı (README §8 hükmü: taşınırsa birleşik kaynak); 3 `test_fx2_*` combinedSource += reminders/reminderSurface (onclick 391 toplamı sabit, 59 gövde modüle taşındı); `ui_boundary` mutation regex ×3 sıkılaştırma (fetchedAt/channelForNotification yanlış pozitif); (9) cache-bust: reminders 20260914a, app.js 20260914c + 4 app_surface + ui_boundary pin; (10) smoke kümesi 20→21 (README sayısı güncellenir — kart güncel haliyle çalıştı). |
| 4 | 2026-09-14 | MON2-03 | 10.277 / 0 / 947 / 928 | 9.771 / 0 / 408 / 928 | syntax (app+core×3+sync); smoke **20/20**; `--gate` PASS (10.300 bütçesi, 9.771/0/408/928); driver+zikr 95/95; verify-state B1/B2/B3; tests/app **53/53** (fx2 6/6); panel 23/23; panel-v2 27/27; quran 9/9; sync 69/69; App.x=554; onclick(kombine)=391; withModule:false PASS | **Yapılan:** 35 yan etkili reminder fn + 51 `App.*reminder*` handler gövdesi → `reminderSurface.js` (1004 satır; MON-50 deseni; app.js 1-liner shim'ler). **Sapmalar:** (1) `appendReminderEvent`+`persistReminderEvent` REM-67 event-adapter dilim sözleşmesi gereği app.js'te **tam gövde** kaldı (fn 37→35; `appendReminderEvent` yine bag'e getter olarak eklendi — modül gövdesi onu çağırır); (2) with(SCOPE) idiomu: with bağlamı modül-yükleme anında statik bağlanır → sabit `Object.create(null)` + `installScopeProperties` (property-getter'lar AYNI objeye; register öncesi gövde fail-closed ReferenceError); (3) kart "126 handler" dedi → 51'i ≥2 satır gövdeliydi (kalan 1-liner alias, olduğu gibi kaldı — kart metni uyumlu); (4) test sözleşme devirleri: acceptance `referenced` shell registry'ler, notification_boundary channel-boundary surfaceSource'ta da, fx2_overlay FX2-16.1 modül `function App_x(){}` + guard'lı sheetClose; (5) app.js delta −506 (kart ≈−1.0k bekliyordu; kalan fark MON2-02'de reminders.js'e gitmişti); (6) cache-bust 20260914d (reminderSurface+app+4 app_surface pin). |
| 6 | 2026-09-14 | MON2-05 | 9.771 / 0 / 408 / 928 | 8.969 / 0 / 408 / 97 | syntax (app+render+sync); smoke **21/21** (73 assertion); `--gate` PASS (9.400 bütçesi, 8.969/0/408/97); driver+zikr **95/95**; verify-state B1/B2/B3; tests/app **52/52** (premium 9 + fx2 6 dahil); panel 23/23; panel-v2 27/27; quran 9/9; sync 69/69; App.x=**554**; onclick fx2 birleşik kaynakta **391**; dump `bugun/rapor/ayarlar/hub` **4/4 BAYT-EŞİT** | **Yapılan:** 37 `*HTML` builder gövdesi (831 kod satırı) → `render.js` (1.843 satır); app.js 1-liner `SEYMA_RENDER.x.apply` shim'ler; dep bag + manifest genişledi (61 fn dep + 14 sabit dep + `find`); render.js `call()`-wrapper'lar + gövdeler export map'e; kalan >2-satır builder **6 fn / 97 satır** (haritaHTML, saveButtonHTML, dailyPhotoCardHTML, bugunHTML(4), headerActionHTML, headerSceneHTML). **Sapmalar:** (1) devir analizi 59 fn dep derken `quranViewBodyHTML` (quranJourneyOverlayHTML'in çıplak helper'ı) eksikti → fn dep 61; (2) taşınan kod 831 satır (devir tahmini 830; analiz aracının kapanış `}` satır sayımı) — stayer toplamı 97; (3) `find` çözücüsü (app.js top-level `SeymaHelpers.find` shim'i, `jsKnown`'da Array metniyle çakıştığından analizden kaçmıştı) taşınan reportHTML/harita gövdelerinde çıplak kullanılıyordu → MON-49 fail-closed bunu yakaladı; render.js wrapper + manifest `'find'` + bag `find:find` eklendi (gerçek runtime bozulmasıydı, shim değil); (4) `App.go` FX2-15 async yolu `SeyFx.isPremiumFxEnabled()` kapısından geçtiği için driver stub'lı timer'larda `premiumAtmosphere=false` senkron yol — dump kanıtı etkilenmedi; (5) bugun dump bayt-eşitliği: `app/core/health.js` `calculateMgNudge`'ta **önceden var** olan `score=Math.round(75+Math.random()*20)` satırı dump skorunu koşum başına değiştiriyordu (90/77 ↔ 83/76) — migration dışı; kanıt için geçici **tmp** driver kopyasında `Math.random` sabitlendi, üretim kodu ve gerçek `.claude/skills` driver'ı değişmedi; 4/4 tab BAYT-EŞİT (31 PASS her iki tarafta); (6) fixture devirleri (K8 — pin gövdeyi izler): `test_today_card_preferences` vacationCardHTML bölümü → renderSource (bitiş `\nfunction hubTilesHTML(){`), `test_modal_focus_containment` zikr/quran bölümleri → renderSource (bitişler `\nfunction quranJourneyHubCardHTML(){` / `\nfunction quranRemoteStatusHTML`), `zikr-harness` z-index:10000 sözleşmesi app.js+render.js birleşik kaynağa; (7) cache-bust: render.js 20260912e→**20260914e**, app.js 20260914d→**20260914e** + 4 app_surface pin'i; (8) `App.x=554` sabit; onclick fx2 combinedSource'ta 391 sabit (render.js combinedSource'ta zaten vardı — fx2 fixture değişikliği gerekmedi); (9) `vacationCardHTML` gövdesinde `ensureVacationSettings` mutation'ı: gövde birebir taşındı, mutation app.js closure'undan dep bag'iyle erişildi (builder kuralı davranışı korundu). |
| 7 | 2026-09-15 | MON2-06 | 8.969 / 0 / 408 / 97 | **7.797 / 0 / 408 / 97** | syntax (app+sync+core×31); smoke **21/21** (73 assertion); `--gate` PASS (8.500 bütçesi, 7.797/0/408/97); driver+zikr **95/95**; verify-state B1/B2/B3; tests/app **52/52**; panel 23/23; panel-v2 27/27; quran 9/9; reminders 21/21; sync 69/69; App.x=**554**; onclick fx2 birleşik kaynakta **391**; dump **6/6 BAYT-EŞİT** (deterministik) | **Yapılan:** quran 52 / zikir 57 / profile+psych 37 gövde ilgili registry'ye (`SeymaQuran`/`SeymaZikr`/`SeymaProfile`), MON-50 appSurface deseni (sloppy IIFE + `with(SCOPE)` + canlı property-getter'lar). Saf gövdeler + K4 handler gövdeleri modülde; app.js'te 1-liner shim (124 fn) ve dep bag'leri kalır. **Kök düzeltme (K4'ün gerçek anlamı):** taşınan 37 yan-etkili gövde önce ÇIPLAK GLOBAL (`document`, `setTimeout(`, `window.SeySync`) kullanıyordu — bu MON-20/21/22/36 saflık sözleşmelerini kırıyordu (4 fixture kırmızı). Gövdeler **dep-bag takma adlarına** çevrildi: `document`→`doc`, `setTimeout(`→`defer(`, `window.SeySync`→`sync`; üç bag'e `doc`/`defer` (+quran/profile `sync`) üyeleri eklendi (`doc:function(){return document;}`, `defer:function(){return setTimeout;}`, `sync:function(){return window.SeySync||null;}`). Böylece domain dosyaları **hiçbir tarayıcı globali adı taşımaz** ve saflık sözleşmeleri zayıflatılmadan geçti. **Sapmalar:** (1) kart "quran 107 fn/750, zikr ≈250, profile ≈217, psych ≈175" bekliyordu; gerçekleşen: taşınan 146 gövde (52+57+37), app.js −1.172 satır — kart tahmini app.js'in TAMAMINI sayıyordu, oysa büyük kısmı MON-21/22/36'da zaten taşınmıştı; (2) zikir.js ilk (strict) bloğunda `viewCall('noteDraftFor',…)` saf delegesi zaten vardı, MON2-06 yüzey bloğu ikinci bir `zikirNoteDraftFor` tanımı ekledi — test pin'i taşınan gövdeye çevrildi (K8); (3) `App_profileAnswer` 160 satır (devir analizi 414 diyordu — analiz aracının kapanış `}` satır sayımı); (4) fixture devirleri (K8 — pin gövdeyi izler): `test_zikir_boundary` zikrTap bölümü → `zikirSource.slice(indexOf('MON2-06'))`, `test_zikir_view_boundary` draft mutation pin'i → aynı dilim, `test_modal_focus_containment` onZikrKeydown → `function App_onZikrKeydown(e){` biçimi, `test_premium_haptics_fx`/`test_premium_voice` appSrc += zikir.js, `test_fx2_overlay_motion` closeX araması zikir/quran kaynaklarını da tarar (combinedSource'a EKLENMEDİ — onclick=391 pini bu dosyalar hariç ölçüldü), `zikr-harness` undo toast metni → `movedSource`; (5) **5. app_surface pin'i** (`test_app_surface_boot_boundary.js:178`) MON2-01..05'te gözden kaçmıştı — bu kartta yakalandı ve senkronlandı; (6) cache-bust: `zikir.js` 20260914a→**20260915a**, `quran.js` 20260904a→**20260915a**, `profile.js` 20260911a→**20260915a**, `app.js` 20260914e→**20260915a** + 5 app_surface pin'i; (7) `--gate` 8.500 bütçesini 703 satır marjla geçti; `App.x=554` ve onclick=391 sabit; (8) dump parity: `bugun`/`reading` ham koşumda tek satırda farklıydı (`calculateMgNudge` `Math.random` skoru, MON2-05'te belgelenmiş önceden-var nondeterminizm) — geçici tmp sabitlemeyle **6/6 bayt-eşit**, üretim kodu değişmedi; (9) kalan 3 saf gövde kartın açık kapsam dışı: `zikrSyncWakeLock` (kart: app.js'te kalır), `quranOnPlayerStateChange`/`quranAttachPlayer` (YouTube iframe API — MON2-07 ağ kapsamı). |
