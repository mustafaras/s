# FX2-01 — Kapsam Denetçisi ve Taban Çizgisi

| | |
|---|---|
| **Dalga** | 0 — Ölçüm |
| **Tip** | Araç (uygulama davranışı değişmez) |
| **Hedef** | Ölçüm altyapısı kurulur; M1–M13 taban çizgisi kaydedilir |
| **Ön koşul** | Yok (serinin ilk kartı) |
| **Sonraki** | FX2-02 |

## NEDEN
FX-1'de 9/9 fixture yeşilken `SeyAudio.tap` **0 kez** çağrılıyordu. Fixture'lar
modülün içini test etti, uygulamaya bağlanıp bağlanmadığını değil. Bu araç
"bağlandı mı?" sorusunu makineye sordurur. **Bu araç olmadan FX2-06 ve sonrası
başlatılamaz.**

## 0. BAŞLAMADAN
```bash
cd /Users/m_ras/Desktop/seyma
git branch --show-current      # premium-fx-gorsel-yuzey olmalı
git status --short             # boş olmalı
ls tools 2>/dev/null || echo "tools/ yok — oluşturulacak"
```

## 1. DEĞİŞTİRİLECEK
| Dosya | Ne |
|---|---|
| `tools/fx-coverage.mjs` | **YENİ** — kapsam denetçisi |
| `premium-fx-plan/.anti-amnesia/coverage.json` | **YENİ** — taban çizgisi |

## 2. DOKUNULMAYACAK (dokunulursa kart iptal)
`app.js` · `app/**` · `index.html` · `tests/**` · `sync.js`

## 3. ADIMLAR

### Adım 1 — Dizin
```bash
mkdir -p tools
```

### Adım 2 — `tools/fx-coverage.mjs` yaz

**Zorunlu davranış:**
- ESM (`.mjs`), yalnız `node:fs` + `node:path`. **Ağ yok, VM yok, yazma yok**
  (`--save` hariç, o da yalnız `coverage.json`).
- `app.js`'i **çalıştırmaz** — saf metin analizi.

**Okuduğu dosyalar:** `app.js`, `app/styles.css`, `app/core/mediaFx.js`,
`app/core/timeTheme.js`, `app/core/state.js`, `index.html`

**Hesaplanacak 13 metrik — sayım kuralları birebir:**

| Kod | Ad | Kural |
|---|---|---|
| M1 | `interactiveTotal` | `app.js`'te `<button` sayısı **+** `onclick=` taşıyan `<div`/`<span`/`<a` sayısı |
| M2 | `pressCovered` | `mediaFx.js`'te `SeyTouch` **ve** `SELECTOR` varsa `M1`, yoksa `0` |
| M3 | `soundWired` | `app.js`'teki açık `SeyAudio.<ad>(` çağrı sayısı **+** (`SeyTouch` varsa `M1`, yoksa 0) |
| M4 | `rippleHosts` | `mediaFx.js` `SeyFx.ripple` gövdesinde `targetEl` geçiyorsa `M1`, yoksa `app.js`'te `onclick="App.…(event` sayısı |
| M5 | `counterAnimated` | `app.js`'te `data-countup` geçen satır sayısı **+** `SeyFx.countUp(` sayısı |
| M6 | `overlayExitAnimated` | `app.js`'te `sheetClose(` sayısı |
| M7 | `motionTokenCompliance` | `styles.css`'te `var(--dur-` veya `var(--ease-` içeren `transition:`/`animation:` bildirimi ÷ toplam `transition:`+`animation:` (2 ondalık) |
| M8 | `defaultOffPremium` | `state.js` `migrate()` içinde `=false` atanan **kimlik** ayarı: `premiumAtmosphere`, `uiSounds`, `richHaptics`, `launchRitual`, `voiceLocalFallback`. (`voiceGuidance`, `ambientSounds` = kullanıcı tercihi, **sayılmaz**.) |
| M9 | `feedbackChannelsIOS` | `styles.css`+`mediaFx.js`'te `.sey-press` varsa +1; `mediaFx.js`'te `resume(` **ve** `createBuffer(` (sessiz buffer) varsa +1. `vibrate` **sayılmaz** (iOS'ta yok). |
| M10 | `pinkTokens` | `styles.css`'te şu hex'lerin toplam sayısı (case-insensitive): `C77D93`, `B55471`, `FFB1CF`, `FCEDEE`, `F1EBFF`, `D96D8B` **+** `rgba(199,125,147` sayısı. (`9C4A5A`/`D68A94` = `--kandil`, **sayılmaz**.) |
| M11 | `paletteFamilies` | `--accent`, `--aeon`, `--read`, `--choc`, `--hijri` tanımlarının hue'larını hesapla (hex→HSL); 40° toleransla kümele; küme sayısı |
| M12 | `ambienceScenes` | `styles.css`'te benzersiz `amb-time-*` + `amb-wx-*` + `amb-season-*` sınıf adı sayısı |
| M13 | `contrastPairs` | `tests/app/` altında adında `contrast` geçen fixture varsa `8`, yoksa `0` (gerçek ölçüm FX2-05'te) |

**Bayraklar:**
- (bayraksız) → hizalı tablo bas: `metrik | taban | şimdi | Δ | eşik | ✅/❌`
- `--save` → mevcut ölçümü `coverage.json`'a taban olarak yaz
- `--gate` → eşiğin altındaki metrik varsa `process.exit(1)`
- `--json` → ham JSON bas

**Eşikler (kod içinde sabit):**
```
M2 ≥ 343   M3 ≥ 200   M4 ≥ 325   M5 ≥ 8    M6 ≥ 10
M7 ≥ 0.80  M8 = 0     M9 ≥ 2     M10 = 0   M11 ≤ 2
M12 ≥ 18   M13 = 8
```

### Adım 3 — Çalıştır ve tabanı yaz
```bash
node tools/fx-coverage.mjs
node tools/fx-coverage.mjs --save
cat premium-fx-plan/.anti-amnesia/coverage.json
```

### Adım 4 — Beklenen tabanla karşılaştır
```
M1  = 387    M2  = 0     M3  = 13    M4  = 0     M5  = 1
M6  = 0      M7  ≈ 0.21  M8  = 4     M9  = 0     M10 = 11
M11 = 5      M12 = 0     M13 = 0
```
**±5 sapma kabul.** Daha fazlaysa **aracı düzelt, belgeyi değil.** Gerçekten
farklıysa `LEDGER.md`'ye farkı yaz ve `coverage.json`'daki değeri esas al.

## 4. DOĞRULAMA
```bash
node --check tools/fx-coverage.mjs
node tools/fx-coverage.mjs --json | python3 -m json.tool >/dev/null && echo "JSON OK"
node tools/fx-coverage.mjs --gate; echo "gate exit=$?"    # 1 beklenir (taban≠eşik)
git status --porcelain | grep -E 'app\.js|app/|index\.html|tests/' && echo "İHLAL" || echo "uygulama dosyasi temiz"
```

## 5. BİTTİ SAYILIR
- [ ] `tools/fx-coverage.mjs` çalışıyor, `node --check` temiz
- [ ] 13 metrik de sayı üretiyor (hiçbiri `NaN`/`undefined` değil)
- [ ] M1≈387, M2=0, M4=0, M8=4, M10=11, M11=5 (±5)
- [ ] `coverage.json` yazıldı ve geçerli JSON
- [ ] `--gate` exit 1 veriyor (henüz hiçbir eşik tutmuyor — doğru)
- [ ] **`git diff --name-only` içinde `app.js`, `app/`, `index.html`, `tests/` YOK**

## 6. GERİ ALMA
Şu iki yeni dosyayı sil: `tools/fx-coverage.mjs`,
`premium-fx-plan/.anti-amnesia/coverage.json`

## 7. KAYIT
`LEDGER.md` + `CURRENT-STATE.md` + `FX2-STATE.json`
(`lastCompletedPrompt:"FX2-01"`, `nextPrompt:"FX2-02"`)
Commit: `fx2: FX2-01 kapsam denetçisi ve taban çizgisi`
