# FX2-06 — `SeyTouch`: Delege Dokunma Katmanı ⭐

| | |
|---|---|
| **Dalga** | 2 — Dokunma |
| **Tip** | Çekirdek entegrasyon |
| **Hedef** | **M2: 0 → ≥ 343 (%95)** |
| **Ön koşul** | FX2-05 tamam |
| **Sonraki** | FX2-07 |

> **Serinin en yüksek etkili kartı.** FX-1'in başaramadığı tek şeyi —
> efektleri gerçekten parmağın altına getirmeyi — tek dosyada çözer.

## NEDEN
361 butonun 277'si satır içi stille yazılmış; ortak sınıf yok, `onclick`'ler
`event` geçmiyor, `render()` her seferinde `#app.innerHTML`'i yıkıyor.
Butonlara tek tek dokunmak (FX-1 yaklaşımı) 361 düzenleme ve 361 regresyon
riski demek. **Delegasyon üçünü birden çözer:** `#root` hiçbir zaman yeniden
kurulmadığı için dinleyici bir kez takılır ve tüm gelecekteki DOM'ları kapsar.

## 0. BAŞLAMADAN
```bash
cd /Users/m_ras/Desktop/seyma
grep -n 'id="root"\|id="app"' index.html          # #root, #app'in ATASI olmalı
grep -n 'function render()' app.js                # ~8951
grep -n 'SeyTimeTheme.apply' app.js               # boot guard deseni — örnek al
grep -oE 'App\.[A-Za-z0-9_]+\s*=[^=]' app.js | grep -oE 'App\.[A-Za-z0-9_]+' | sort -u | wc -l   # 717 NOT ET
```

## 1. DEĞİŞTİRİLECEK
| Dosya | Ne |
|---|---|
| `app/core/mediaFx.js` | `window.SeyTouch` eklenir (dosya sonuna) |
| `app.js` | **tek satır** boot çağrısı |
| `index.html` | `mediaFx.js?v=` + `app.js?v=` bump |

## 2. DOKUNULMAYACAK
Hiçbir `App.*` handler'ı · hiçbir buton markup'ı · `render()` · `paint()` ·
`app/styles.css` (FX2-07'nin işi)

## 3. ADIMLAR

### Adım 1 — `mediaFx.js` sonuna `SeyTouch` ekle

```js
  // ── FX2-06: Delege dokunma katmanı ──────────────────────────────────────
  // #root'a takılır (ASLA #app'e — o her render'da innerHTML ile yıkılır).
  // Tek dinleyici, 361 butonun tamamını ve gelecekte üretilecek tüm DOM'u
  // kapsar. preventDefault ÇAĞRILMAZ: kaydırma asla bozulmaz.
  var TOUCH_SELECTOR = 'button,[role="button"],[data-fx],a[href],[onclick]';
  var _tActive = null, _tLastAt = 0, _tX = 0, _tY = 0;

  function touchDown(e){
    try{
      if (e.pointerType === 'mouse' && e.button !== 0) return;
      var el = e.target && e.target.closest ? e.target.closest(TOUCH_SELECTOR) : null;
      if (!el) return;
      if (el.disabled || el.getAttribute('aria-disabled') === 'true') return;
      if (el.matches && el.matches('input,textarea,select')) return;
      if (el.dataset && el.dataset.fx === 'none') return;
      var now = Date.now();
      if (now - _tLastAt < 40) return;        // gürültü kapısı
      _tLastAt = now; _tActive = el;
      _tX = e.clientX || 0; _tY = e.clientY || 0;
      el.classList.add('sey-press');
      // FX çağrıları: her biri kendi gating'ini yapar, hata tıklamayı engellemez
      try{ if (window.SeyAudio && window.SeyAudio.tap) window.SeyAudio.tap(); }catch(err){}
      try{ if (window.SeyHaptics && window.SeyHaptics.tap) window.SeyHaptics.tap(); }catch(err){}
      try{ if (window.SeyFx && window.SeyFx.ripple) window.SeyFx.ripple(e, null, el); }catch(err){}
    }catch(err){}
  }
  function touchUp(){
    if (_tActive){ try{ _tActive.classList.remove('sey-press'); }catch(e){} _tActive = null; }
  }
  function touchMove(e){
    if (!_tActive) return;
    var dx = (e.clientX || 0) - _tX, dy = (e.clientY || 0) - _tY;
    if (dx*dx + dy*dy > 100) touchUp();      // >10px sapma = kaydırma, basmayı iptal et
  }

  window.SeyTouch = {
    SELECTOR: TOUCH_SELECTOR,
    _installed: false,
    install: function(rootEl){
      if (window.SeyTouch._installed) return true;      // idempotent
      var root = rootEl || document.getElementById('root');
      if (!root) return false;
      root.addEventListener('pointerdown', touchDown, { passive: true, capture: true });
      window.addEventListener('pointerup', touchUp, { passive: true });
      window.addEventListener('pointercancel', touchUp, { passive: true });
      window.addEventListener('pointermove', touchMove, { passive: true });
      window.SeyTouch._installed = true;
      return true;
    }
  };
```

### Adım 2 — `app.js` boot bağlantısı (TEK satır)

**Bul:** boot sonundaki `render()` ilk çağrısı (`grep -n 'SeyTimeTheme.apply' app.js`
ile bulduğun guard deseninin yakını).
**Ekle** — aynı `try/catch` deseninde:
```js
try{ if(window.SeyTouch && typeof window.SeyTouch.install==='function') window.SeyTouch.install(document.getElementById('root')); }catch(e){}
```

> ⚠️ **`render()` içine KOYMA.** Orası her render'da çalışır. Boot'ta bir kez
> yeterli (`install` yine de idempotenttir).

### Adım 3 — Cache bump
`index.html`: `app/core/mediaFx.js?v=` ve `app.js?v=` bir sonraki harfe.

## KRİTİK UYARILAR
| Tuzak | Sonuç | Kural |
|---|---|---|
| `touchstart` + `mousedown` birlikte dinlemek | iOS'ta ses **iki kez** çalar | **Yalnız Pointer Events** |
| Dinleyiciyi `#app`'e takmak | İlk `render()`'da düşer | **`#root`** |
| `preventDefault()` | Kaydırma ölür | **Asla çağırma** |
| `passive:false` | Scroll jank | **Hepsi `passive:true`** |
| FX çağrısını `try` dışında bırakmak | Ses hatası tıklamayı öldürür | **Her biri kendi `try`'ında** |

**Not:** `SeyAudio.tap` bu kartta hâlâ 180 ms "bip"tir. Doğru ses FX2-12'de
gelir. Bu kart **bağlantıyı** kurar, sesi güzelleştirmez.
`.sey-press` CSS'i FX2-07'de gelir — şimdilik sınıf takılır, görsel etki yok.

## 4. DOĞRULAMA
```bash
node --check app/core/mediaFx.js && node --check app.js
grep -c 'SeyTouch'          app/core/mediaFx.js   # >= 3
grep -c 'SeyTouch'          app.js                # 1  (tek boot satırı)
grep -c 'addEventListener'  app/core/mediaFx.js   # 4
grep -c 'preventDefault'    app/core/mediaFx.js   # 0  <- KRİTİK
grep -c 'passive: true\|passive:true' app/core/mediaFx.js  # >= 4
node .claude/skills/run-seyma/driver.mjs 2>&1 | grep -c '^FAIL'   # 0
node .claude/skills/run-seyma/zikr-harness.mjs 2>&1 | tail -1     # 95/95
for f in tests/app/*.js; do node "$f" >/dev/null 2>&1 || echo "FAIL $f"; done
node tools/fx-coverage.mjs                        # M2 >= 343
```

**Değişmezlik (S5):**
```bash
grep -oE 'App\.[A-Za-z0-9_]+\s*=[^=]' app.js | grep -oE 'App\.[A-Za-z0-9_]+' | sort -u | wc -l   # 717
grep -o 'onclick=' app.js | wc -l                                                                # 391
```

## 5. BİTTİ SAYILIR
- [ ] **M2 ≥ 343** — bu kartın tek gerçek testi
- [ ] `App.*` = 717, `onclick` = 391 (hiçbir handler dokunulmadı)
- [ ] `preventDefault` sayısı **0**
- [ ] Dinleyici `#root`'ta, `#app`'te değil
- [ ] `install()` iki kez çağrılınca ikinci sefer no-op
- [ ] 4 FX çağrısının hepsi `try/catch` içinde
- [ ] driver + zikr + tüm `tests/app` yeşil

## 6. GERİ ALMA
Git ile geri al: `app/core/mediaFx.js`, `app.js`, `index.html`

## 7. KAYIT
`FX2-STATE.json`: `lastCompletedPrompt:"FX2-06"`, `nextPrompt:"FX2-07"`
Commit: `fx2: FX2-06 delege dokunma katmanı (SeyTouch)`
