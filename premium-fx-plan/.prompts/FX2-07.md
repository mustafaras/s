# FX2-07 — Basma Durumu CSS'i

| | |
|---|---|
| **Dalga** | 2 — Dokunma |
| **Tip** | CSS + hata düzeltmesi |
| **Hedef** | M2'nin görsel karşılığı · `.surface:active` hatası giderilir |
| **Ön koşul** | FX2-06 tamam |
| **Sonraki** | FX2-08 |

## NEDEN
FX2-06 `.sey-press` sınıfını takıyor ama karşılığı yok. Ayrıca mevcut
`.surface:active{transform:scale(.97)}` **hatalı**: `.surface` bir **kart**
sınıfı (70 kullanım) — kartın içindeki butona basınca **bütün kart** küçülüyor.
Üstelik `-webkit-tap-highlight-color:transparent` 6 yerde tanımlı; oralarda
tarayıcının kendi parıltısı da söndürülmüş, yerine hiçbir şey konmamış.

## 0. BAŞLAMADAN
```bash
cd /Users/m_ras/Desktop/seyma
grep -n 'surface:active' app/styles.css
grep -n 'press-scale\|press-dim' app/styles.css    # FX2-02'de tanımlanmış olmalı
```

## 1. DEĞİŞTİRİLECEK
`app/styles.css` · `index.html` (bump)

## 2. DOKUNULMAYACAK
`app.js` · `app/core/**` · `.surface:hover` (FX2-24'ün işi)

## 3. ADIMLAR

### Adım 1 — `.sey-press` kuralı (FX bölümüne)
```css
/* FX2-07: SeyTouch'ın taktığı basma durumu. Markup değişmez.
   ÖNEMLİ: premiumAtmosphere'a BAĞLI DEĞİLDİR — basma geri bildirimi
   erişilebilirliktir, süs değil (SAFEGUARDS §2). */
.sey-press{
  transform: scale(var(--press-scale));
  filter: brightness(var(--press-dim));
  transition: transform var(--dur-1) var(--ease-out),
              filter    var(--dur-1) var(--ease-out);
  -webkit-tap-highlight-color: transparent;
}
@media (prefers-reduced-motion: reduce){
  .sey-press{ transform: none; }   /* parlaklık kalır, hareket gider */
}
```

### Adım 2 — Hatalı kuralı kaldır
**Sil:** `.surface:active{transform:scale(.97);}`
**Sil:** reduced-motion bloğundaki `.surface:active` parçası
(`.surface,.surface:hover,.surface:active{...}` → `.surface:active` çıkarılır)

> Yerine hiçbir şey konmaz. Basma geri bildirimi artık gerçek dokunulan
> elemana gidiyor.

### Adım 3 — Cache bump

## 4. DOĞRULAMA
```bash
grep -c 'sey-press'      app/styles.css   # >= 3
grep -c 'surface:active' app/styles.css   # 0  <- KRİTİK
grep -c 'press-scale'    app/styles.css   # >= 2 (tanım + kullanım)
node .claude/skills/run-seyma/driver.mjs 2>&1 | grep -c '^FAIL'   # 0
node tests/app/test_premium_reduced_motion.js
for f in tests/app/*.js; do node "$f" >/dev/null 2>&1 || echo "FAIL $f"; done
node tools/fx-coverage.mjs                # M9 >= 1 (görsel kanal açıldı)
```

## 5. BİTTİ SAYILIR
- [ ] `.sey-press` tanımlı; `--press-scale`/`--press-dim` token'larını kullanıyor
- [ ] `.surface:active` **tamamen** kaldırıldı (reduced-motion bloğu dâhil)
- [ ] Koyu temada parlatma (`--press-dim:1.12`) çalışıyor
- [ ] Yalnız `transform` + `filter` animasyonu (I8)
- [ ] **M9 ≥ 1**
- [ ] `app.js` değişmedi

## 6. GERİ ALMA
Git ile geri al: `app/styles.css`, `index.html`

## 7. KAYIT
`FX2-STATE.json`: `lastCompletedPrompt:"FX2-07"`, `nextPrompt:"FX2-08"`
Commit: `fx2: FX2-07 basma durumu ve kart-küçültme düzeltmesi`
