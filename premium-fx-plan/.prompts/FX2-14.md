# FX2-14 — Ses Motoru Fixture'ı (Dalga 3 kapanışı)

| | |
|---|---|
| **Dalga** | 3 — Ses Kimliği |
| **Tip** | Test |
| **Hedef** | Ses zincirini kilitle |
| **Ön koşul** | FX2-13 tamam |
| **Sonraki** | FX2-15 |

## 1. DEĞİŞTİRİLECEK
`tests/app/test_fx2_audio_engine.js` **(YENİ)**

## 3. ADIMLAR

Fixture **gerçek `mediaFx.js`'i** `node:vm`'de **sahte `AudioContext`** ile
yükler. Sahte bağlam her `create*` çağrısını ve her `connect()` kenarını
kaydeder; testler grafiği bunun üzerinden doğrular.

### 12 test grubu
1. **Graf şekli:** `busGain → compressor → waveShaper → destination` yolu var.
2. **Reverb:** `createConvolver` bir kez; `buffer` kod içinde üretilmiş
   (ağ/dosya erişimi yok).
3. **Tek kurulum:** 10 ses çalınca `createDynamicsCompressor` **1** kez.
4. **Polifoni:** 12 ses arka arkaya → aynı anda aktif kaynak ≤ 6.
5. **Varyasyon:** aynı ses 5 kez → en az 3 farklı `detune` değeri.
6. **Süreler:** `tap` ≤ 60 ms, `tick` ≤ 30 ms.
7. **`bell` inharmonik:** ≥ 5 osilatör; oranlar `[1, 2.76, 5.40, …]`.
8. **`warning` sawtooth değil.**
9. **Gating matrisi — 5 grup × 4 koşul:**
   `premiumAtmosphere:false` → hepsi sessiz;
   `uiSounds:false` → UI sesleri sessiz;
   reduced-motion → `tap`/`tick` **çalar**, diğerleri sessiz;
   quiet-time 23–07 → voice/ambient sessiz, UI sesleri **çalar**.
10. **iOS kilidi:** `{once:true}` dinleyici var; `ctx.state='suspended'` iken
    ses çağrısı **hata fırlatmaz** ve sessiz kalır.
11. **Ağ yok:** `fetch` yalnız bulut TTS yolunda; UI ses yolunda hiç.
12. **API korunumu (I2):** `tap, success, warning, bell, voice, speakLocal,
    isVoiceEnabled, isQuietTime, isAudible, greeting, guides, ambient, ctx`
    üyeleri duruyor.

## 4. DOĞRULAMA
```bash
node tests/app/test_fx2_audio_engine.js
node tests/app/test_premium_audio_fx.js
node tools/fx-coverage.mjs --gate 2>&1 | tail -20    # M3, M9 ✅
for f in tests/app/*.js; do printf '%-48s ' "$(basename $f)"; node "$f" >/dev/null 2>&1 && echo PASS || echo FAIL; done
```

## 5. BİTTİ SAYILIR
- [ ] 12 test grubu PASS
- [ ] Sahte `AudioContext`, **gerçek** modül
- [ ] **Dalga 3 kapandı:** M3 ≥ 200, M9 ≥ 2

## 6. GERİ ALMA
Yeni fixture dosyasını sil: `tests/app/test_fx2_audio_engine.js`

## 7. KAYIT
`FX2-STATE.json`: `lastCompletedPrompt:"FX2-14"`, `nextPrompt:"FX2-15"`,
`currentPhase:"Dalga 4 — Hareket Sistemi"`
Commit: `fx2: FX2-14 ses motoru fixture'ı (Dalga 3 kapanışı)`
