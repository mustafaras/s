# FX2-15 — Sekme Geçiş Motoru

| | |
|---|---|
| **Dalga** | 4 — Hareket |
| **Tip** | Entegrasyon |
| **Hedef** | 7 sekmenin 7'sinde gerçek çıkış+giriş geçişi |
| **Ön koşul** | FX2-14 tamam |
| **Sonraki** | FX2-16 |

## NEDEN
`render()` `app.innerHTML = html` ile ekranı yıkıyor; çıkış animasyonu
imkânsız, geçiş "flaş" gibi. Şu an `App.go` yalnız `SeyFx.enter` çağırıyor
(giriş var, **çıkış yok**). Premium his çiftten doğar.

## 0. BAŞLAMADAN
```bash
cd /Users/m_ras/Desktop/seyma
grep -n 'App.go=function' app.js          # ~5863
sed -n "$(grep -n 'App.go=function' app.js | cut -d: -f1),+26p" app.js   # mevcut gövdeyi OKU
```

## 1. DEĞİŞTİRİLECEK
`app.js` (yalnız `App.go` gövdesi) · `app/styles.css` · `index.html` (bump)

## 2. DOKUNULMAYACAK
`render()` · `paint()` · scroll/odak restorasyonu (I7) · diğer handler'lar

## 3. ADIMLAR

### Adım 1 — CSS (FX bölümü)
```css
/* FX2-15: sekme geçişi. #app innerHTML ile yıkıldığı için çıkış animasyonu
   render()'dan ÖNCE oynatılır. */
#app{ transition: opacity var(--dur-3) var(--ease-out),
                  transform var(--dur-3) var(--ease-out); }
#app.sey-leaving { opacity:0; transform:translateY(6px);
  transition: opacity var(--dur-2) var(--ease-in),
              transform var(--dur-2) var(--ease-in); }
#app.sey-entering{ opacity:0; transform:translateY(-6px); }
@media (prefers-reduced-motion: reduce){
  #app,#app.sey-leaving,#app.sey-entering{
    transition:none!important; opacity:1!important; transform:none!important; }
}
```

### Adım 2 — `App.go` sarmalanır (gövde AYNEN korunur)

```js
App.go=function(id,event){
  var tabChanged = ui.tab !== id;
  // MEVCUT GÖVDE — tek karakter değişmeden bir fonksiyona alınır
  function commitGo(){
    if(id==='saygi'&&ui.tab!=='saygi'&&typeof quranAdvanceVerseIndex==='function') quranAdvanceVerseIndex();
    if(id==='mesaj'&&ui.tab!=='mesaj') ui.aeonScrollBottom=true;
    ui.tab=id; render();
    var sc=document.querySelector('[data-scroll]'); if(sc&&id!=='mesaj') sc.scrollTop=0;
    tryLocNudge('tab');
    if(tabChanged && window.SeyFx && typeof window.SeyFx.enter==='function'){
      window.SeyFx.enter('#app .surface, #app .card, #app .bento', 40);
    }
  }
  var appEl = document.getElementById('app');
  var animOk = tabChanged && appEl && window.SeyFx
            && typeof window.SeyFx.isPremiumFxEnabled==='function'
            && window.SeyFx.isPremiumFxEnabled();
  if(!animOk){ commitGo(); return; }

  // Yeniden giriş koruması: geçiş sürerken yeni istek gelirse bekleyeni iptal et
  if(App._goTimer){ clearTimeout(App._goTimer); App._goTimer=null;
    try{ appEl.classList.remove('sey-leaving'); }catch(e){} }

  var done=false;
  function finish(){
    if(done) return; done=true;
    if(App._goTimer){ clearTimeout(App._goTimer); App._goTimer=null; }
    appEl.removeEventListener('transitionend', finish);
    commitGo();
    var el=document.getElementById('app');
    if(el){
      el.classList.remove('sey-leaving');
      el.classList.add('sey-entering');
      requestAnimationFrame(function(){ requestAnimationFrame(function(){
        el.classList.remove('sey-entering');
      }); });
    }
  }
  appEl.classList.add('sey-leaving');
  appEl.addEventListener('transitionend', finish, { once:true });
  App._goTimer = setTimeout(finish, 200);   // AĞ: transitionend gelmezse
};
```

### Adım 3 — `App.go` **yeni bir isim değil** — mevcut handler yeniden yazıldı
`App._goTimer` bir **iç durum alanı**, handler değil. `App.*` benzersiz
sayısı **717 + 1 = 718** olur ve bu **belgelenir**.

### Adım 4 — Ses eklemesi YOK
`data-fx="nav"` (FX2-09) zaten `nav` sesini çalıyor. Buraya ses eklemek
**çift ses** yapar.

### Adım 5 — Cache bump

## KRİTİK: TIMEOUT AĞI
`transitionend` gelmeyebilir (arka plan sekmesi, düşük güç modu,
`display:none`, reduced-motion). **Timeout olmadan uygulama kilitlenir.**
`Promise.race` **kullanma** — ES5 uyumu için basit bayrak + `clearTimeout`.

## 4. DOĞRULAMA
```bash
node --check app.js
grep -c 'sey-leaving' app.js app/styles.css      # her ikisinde >= 1
grep -c '_goTimer'    app.js                     # >= 4
grep -c 'requestAnimationFrame' app.js           # arttı
node .claude/skills/run-seyma/driver.mjs 2>&1 | grep -c '^FAIL'   # 0
node .claude/skills/run-seyma/driver.mjs --dump bugun > "$TMPDIR/fx15.html"
grep -c 'sey-bottomnav' "$TMPDIR/fx15.html"      # >0 — nav hâlâ üretiliyor
node tests/app/test_premium_reduced_motion.js
for f in tests/app/*.js; do node "$f" >/dev/null 2>&1 || echo "FAIL $f"; done
```

**Değişmezlik:** `App.*` = **718** (+1 `_goTimer`, belgelendi), `onclick` = 391.

## 5. BİTTİ SAYILIR
- [ ] `render()` gövdesi değişmedi (I7)
- [ ] Timeout ağı var; `transitionend` gelmese de akış devam ediyor
- [ ] Hızlı ardışık sekme değişimi **kilitlenmiyor**
- [ ] reduced-motion / premium kapalı → senkron eski davranış
- [ ] Sekme değişiminde **tek** ses
- [ ] `App.*` = 718 (artış belgelendi)

## 6. GERİ ALMA
Git ile geri al: `app.js`, `app/styles.css`, `index.html`

## 7. KAYIT
`FX2-STATE.json`: `lastCompletedPrompt:"FX2-15"`, `nextPrompt:"FX2-16"`,
`appSurfaceBaseline.AppHandlers: 718`
Commit: `fx2: FX2-15 sekme geçiş motoru`
