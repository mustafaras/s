# FX2-19 — `SeyAmbience` Çekirdeği 🌦️

| | |
|---|---|
| **Dalga** | 5 — Canlı Zemin |
| **Tip** | Yeni motor (saf mantık) |
| **Hedef** | Sahne hesaplayıcı kurulur; henüz görsel değişiklik YOK |
| **Ön koşul** | FX2-18 tamam |
| **Sonraki** | FX2-20 |

## NEDEN
Kullanıcı: *"değişken arka planlar ekleyelim; bu FX planda vardı ama sonradan
ne olduysa ya uygulanmadı ya kayboldu."* Doğru — FX-1 `PLAN.md` §4.4'te
"Yağmur/bulut modu … **gelecekte hava API'si varsa**" yazıyordu ve FX-P-88
("hava modu") bloklu bırakıldı.

> **O gerekçe artık geçersiz: hava API'si gelecekte değil, uygulamada.**

`app.js` `fetchWeather()` open-meteo'dan çekip `data.weather` altına yazıyor.
`data.weather.spots[0]` şunları taşıyor: `code` (WMO), `isDay`, `precip`,
`temp`, `sunrise`, `sunset`, `uv`, `wind`.
**Canlı zemin için tek bir yeni ağ çağrısı bile gerekmiyor.**

## 0. BAŞLAMADAN
```bash
cd /Users/m_ras/Desktop/seyma
grep -n 'data.weather=' app.js                      # ~9940 — veri şeklini gör
sed -n '9920,9942p' app.js                          # spots[] alanları
cat app/core/timeTheme.js                           # 65 satır, tamamını oku
grep -n 'SeyTimeTheme' app.js                       # 2 çağrı: apply, applySeasonal
```

## 1. DEĞİŞTİRİLECEK
| Dosya | Ne |
|---|---|
| `app/core/timeTheme.js` | `window.SeyAmbience` eklenir (dosya sonuna) |
| `index.html` | `timeTheme.js?v=` bump |

## 2. DOKUNULMAYACAK
`app.js` (bu kartta hiç açılmaz) · `SeyTimeTheme.classForHour/apply/
seasonalClass/applySeasonal` (I2 — hepsi korunur) · `app/styles.css`
(FX2-20'nin işi) · `fetchWeather` · `data.weather`

## 3. ADIMLAR

### Adım 1 — Saf sahne hesaplayıcı

`timeTheme.js` sonuna, **`SeyTimeTheme`'e dokunmadan**:

```js
  // ── FX2-19: SeyAmbience — canlı zemin sahnesi ────────────────────────────
  // Tek sorumluluk: #root'a hangi sahne sınıflarının yazılacağını HESAPLAMAK.
  // Boyama tamamen CSS'te. Ağ çağrısı YOK — data.weather zaten dolu.
  function wx(){
    try{
      var d = window.SeymaState && window.SeymaState.data;
      var s = d && d.weather && d.weather.spots && d.weather.spots[0];
      return s || null;
    }catch(e){ return null; }
  }

  // WMO kodu → 8 hava sahnesi
  var WX_MAP = [
    ['amb-wx-clear',   [0,1]],
    ['amb-wx-cloud',   [2,3]],
    ['amb-wx-fog',     [45,48]],
    ['amb-wx-drizzle', [51,53,55,56,57]],
    ['amb-wx-rain',    [61,63,65,66,67,80,81,82]],
    ['amb-wx-snow',    [71,73,75,77,85,86]],
    ['amb-wx-storm',   [95,96,99]]
  ];
  function weatherClass(code){
    if (code == null || isNaN(code)) return 'amb-wx-none';
    for (var i=0;i<WX_MAP.length;i++){
      if (WX_MAP[i][1].indexOf(Number(code)) >= 0) return WX_MAP[i][0];
    }
    return 'amb-wx-none';
  }

  // Şiddet 0–1: yağış + rüzgârdan türer. CSS animasyon hızı/opaklığı bundan.
  function intensity(s){
    if (!s) return 0.35;
    var p = Math.min(Number(s.precip)||0, 8) / 8;      // 0–8 mm
    var w = Math.min(Number(s.wind)||0, 40) / 40;      // 0–40 km/s
    return Math.max(0.15, Math.min(1, p*0.7 + w*0.3));
  }

  // Günün tarihinden türeyen deterministik 0–1: gün içinde SABİT, gün gün değişir.
  function seed(d){
    var t = d || new Date();
    var k = t.getFullYear()*10000 + (t.getMonth()+1)*100 + t.getDate();
    var x = Math.sin(k) * 10000;
    return x - Math.floor(x);
  }

  window.SeyAmbience = {
    weatherClass: weatherClass,
    intensity: intensity,
    seed: seed,
    // Saf: girdi verilirse onu kullanır, verilmezse canlı veriyi okur.
    // FX2-20 zaman katmanını, FX2-22 mevsim katmanını ekler.
    scene: function(now, spot){
      var s = (spot !== undefined) ? spot : wx();
      return {
        time:    'amb-time-day',                 // FX2-20 gerçek değeri koyacak
        weather: weatherClass(s && s.code),
        season:  '',                             // FX2-22 dolduracak
        isDay:   s ? s.isDay !== false : true,
        intensity: intensity(s),
        seed:    seed(now)
      };
    },
    apply: function(){ return false; }            // FX2-20'de gerçek gövde gelir
  };
```

### Adım 2 — Cache bump
`index.html`: `app/core/timeTheme.js?v=` bir sonraki harfe.

> **Bu kart bilinçli olarak görsel değişiklik üretmez.** `apply()` henüz
> `false` dönüyor. Amaç: saf, test edilebilir bir çekirdek kurmak.
> M12 hâlâ 0 olacaktır — **bu beklenen davranıştır**, S8 ihlali değildir.

## 4. DOĞRULAMA
```bash
node --check app/core/timeTheme.js
grep -c 'SeyAmbience'   app/core/timeTheme.js     # >= 3
grep -c 'SeyTimeTheme'  app/core/timeTheme.js     # değişmedi (mevcut sayı)
grep -c 'fetch('        app/core/timeTheme.js     # 0  <- KRİTİK: yeni ağ yok
grep -c 'setInterval'   app/core/timeTheme.js     # 0  <- KRİTİK: batarya
node .claude/skills/run-seyma/driver.mjs 2>&1 | grep -c '^FAIL'   # 0
node tests/app/test_premium_time_theme.js                         # 53/53 KORUNMALI
for f in tests/app/*.js; do node "$f" >/dev/null 2>&1 || echo "FAIL $f"; done
```

**Saf fonksiyon hızlı testi:**
```bash
node -e "
global.window={}; global.document={getElementById:()=>null};
require('/Users/m_ras/Desktop/seyma/app/core/timeTheme.js');
const A=window.SeyAmbience;
console.log('clear  0 ->', A.weatherClass(0));
console.log('rain  63 ->', A.weatherClass(63));
console.log('snow  75 ->', A.weatherClass(75));
console.log('storm 95 ->', A.weatherClass(95));
console.log('yok null->', A.weatherClass(null));
console.log('siddet {precip:4,wind:20} ->', A.intensity({precip:4,wind:20}).toFixed(2));
console.log('seed determinist:', A.seed(new Date('2026-09-06'))===A.seed(new Date('2026-09-06')));
"
```
Beklenen: `amb-wx-clear`, `amb-wx-rain`, `amb-wx-snow`, `amb-wx-storm`,
`amb-wx-none`, şiddet ≈ `0.50`, seed `true`.

## 5. BİTTİ SAYILIR
- [ ] `SeyAmbience.weatherClass/intensity/seed/scene` çalışıyor
- [ ] 8 WMO sahnesinin hepsi doğru eşleşiyor
- [ ] `seed()` aynı gün için **aynı** değeri dönüyor
- [ ] `SeyTimeTheme`'in dört fonksiyonu **dokunulmadı** (I2)
- [ ] `timeTheme.js`'te `fetch` = 0, `setInterval` = 0
- [ ] `test_premium_time_theme.js` 53/53
- [ ] `app.js` açılmadı

## 6. GERİ ALMA
Git ile geri al: `app/core/timeTheme.js`, `index.html`

## 7. KAYIT
`FX2-STATE.json`: `lastCompletedPrompt:"FX2-19"`, `nextPrompt:"FX2-20"`
Commit: `fx2: FX2-19 SeyAmbience çekirdeği`
