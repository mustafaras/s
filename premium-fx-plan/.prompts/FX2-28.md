# FX2-28 — Kapanış ve Doküman Senkronu

| | |
|---|---|
| **Dalga** | 7 — Kapanış |
| **Tip** | Belge |
| **Hedef** | Tek doğru kaynak · ölü bağlantı 0 |
| **Ön koşul** | FX2-27 tamam (0 FAIL + gate PASS) |
| **Sonraki** | — (seri biter) |

## 3. ADIMLAR

### Adım 1 — `deliverables/FX2-KAPANIS.md`
- Seri özeti (28 kart, 8 dalga, yerel commit sayısı)
- **Öncesi/sonrası kapsam tablosu** (M1–M13) — asıl kanıt
- **Renk kararı:** pembe → Şampanya Altını + Füme; altın merdiveni
  `--gold-1..5`; palet 5 aileden 2'ye indi
- **Canlı zemin:** 4 zaman × 8 hava × 6 mevsim = **192 kombinasyon**;
  `data.weather` kullanıldı, **yeni ağ çağrısı yok**
- Nihai API envanteri: `SeyTouch`, `SeyAmbience`, `SeyAudio` (11+ ses),
  `SeyFx` (+`sheetClose`, `sweepCounters`, `bindAuroraParallax`),
  `SeyHaptics`, `SeyTimeTheme` (korundu)
- Yeni/değişen `settings` alanları ve varsayılanları
- **Bilinen sınırlar** (FX2-27 §6'dan)
- Push/merge/deploy: **yapılmadı, kullanıcı onayı bekliyor**

### Adım 2 — `README.md` güncelle
Durum tablosu, kapsam özeti, "seri tamamlandı" bölümü.

### Adım 3 — `.anti-amnesia/` finalize
`FX2-STATE.json`: `implementationComplete:true`, `pushedToRemote:false`,
`blockedPrompt:null`, `nextPrompt:null`.
`CURRENT-STATE.md` son bölüm + `LEDGER.md` son satır.

### Adım 4 — Kök yönlendirmeleri (`CLAUDE.md` **ve** `AGENTS.md`)
- Premium FX maddesi: giriş noktası `TESHIS.md` → `PLAN-FX2.md`
- `RENK-VE-ZEMIN.md` eklenir (renk kararı + canlı zemin sözleşmesi)
- `MODULARIZATION.md` referansı **korunur**
  (`tests/app/test_modularization_boundary.js` ona bağlı)
- Repo düzeni bölümünde `tools/fx-coverage.mjs` ve yeni fixture'lar anılır
- İki dosya **birebir aynı** tutulur

### Adım 5 — `docs/GELISTIRME-PLANI.md`
Durum tablosuna premium FX satırı eklenir/güncellenir.

### Adım 6 — Ölü bağlantı taraması
```bash
cd /Users/m_ras/Desktop/seyma
grep -rhoE 'premium-fx-plan/[A-Za-z0-9_./-]+' --include='*.md' --include='*.json' . \
  | sed 's/[.,)]*$//' | sort -u | while read p; do [ -e "$p" ] || echo "ÖLÜ: $p"; done
```
Çıktı **boş** olmalı. (`.anti-amnesia/LEDGER.md` içindeki tarihsel FX-1
dosya adları istisna — append-only, düzenlenmez.)

### Adım 7 — Son kontrol
```bash
node tools/fx-coverage.mjs --gate && echo "GATE PASS"
for f in tests/app/*.js; do node "$f" >/dev/null 2>&1 || echo "FAIL $f"; done
node tests/app/test_modularization_boundary.js | tail -2
git log --oneline | grep -c '^[0-9a-f]* fx2:'    # 28 commit
```

## 5. BİTTİ SAYILIR
- [ ] `FX2-KAPANIS.md` **kapsam tablosu içeriyor**
- [ ] `CLAUDE.md` + `AGENTS.md` güncel ve birbirinin aynısı
- [ ] Ölü bağlantı **yok**
- [ ] `MODULARIZATION.md` dokunulmadı; boundary fixture yeşil
- [ ] `monolit-bolumlenme-plan/` referansları hâlâ çözülüyor
- [ ] `--gate` PASS
- [ ] **Push/merge/deploy YAPILMADI**

## 6. GERİ ALMA
Git ile geri al: değişen belge dosyaları

## 7. KAYIT
`FX2-STATE.json`: `implementationComplete:true`, `nextPrompt:null`
Commit: `fx2: FX2-28 kapanış ve doküman senkronu`

---

> **Seri burada biter.** Merge/deploy ayrı ve açık kullanıcı onayı gerektirir.
> Cihaz kabulü (K3) yalnız kullanıcıdan gelir.
