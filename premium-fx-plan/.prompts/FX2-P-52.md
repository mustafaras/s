# FX2-P-52 — Tam Regresyon ve Kapsam Raporu

**Dalga:** 5 · **Tip:** salt-okur QA · **Hedef:** 0 FAIL + kapsam kanıtı

## Kural
**Hiçbir kaynak dosya değiştirilmez.** Yalnız
`premium-fx-plan/deliverables/FX2-KAPSAM-RAPORU.md` üretilir.

## Adımlar

1. **Syntax:**
   ```bash
   for f in app.js sync.js sw.js panel/panel.js panel/panelCoverageManifest.js \
            app/core/state.js app/core/mediaFx.js app/core/timeTheme.js \
            app/core/syncGlue.js app/core/helpers.js tools/fx-coverage.mjs; do
     node --check "$f" >/dev/null && echo "OK   $f" || echo "FAIL $f"; done
   ```

2. **Harness:**
   ```bash
   node .claude/skills/run-seyma/driver.mjs
   node .claude/skills/run-seyma/zikr-harness.mjs        # 95/95
   node .claude/skills/run-seyma/verify-state-migration-boundary.mjs
   node .claude/skills/run-seyma/verify-state-helper-boundary.mjs
   node .claude/skills/run-seyma/verify-state-adapter-contract.mjs
   ```

3. **Fixture aileleri:**
   ```bash
   for f in tests/app/*.js; do printf '%-52s ' "$(basename $f)"; node "$f" >/dev/null 2>&1 && echo PASS || echo FAIL; done
   for f in tests/panel/*.js tests/panel-v2/test_panel_v2_*.js tests/quran/*.js; do
     printf '%-52s ' "$(basename $f)"; node "$f" >/dev/null 2>&1 && echo PASS || echo FAIL; done
   node tests/reminders/run-reminder-smoke.mjs
   ```

4. **Kapsam kapısı:**
   ```bash
   node tools/fx-coverage.mjs --gate && echo "GATE PASS"
   node tools/fx-coverage.mjs --json
   ```

5. **Değişmezler (I1–I8):**
   ```bash
   grep -oE 'App\.[A-Za-z0-9_]+\s*=[^=]' app.js | grep -oE 'App\.[A-Za-z0-9_]+' | sort -u | wc -l
   grep -o 'onclick=' app.js | wc -l
   grep -c '<script src="app.js' index.html          # 1
   grep -c 'preventDefault' app/core/mediaFx.js      # 0
   git diff --stat main -- sync.js                   # boş olmalı
   ```

6. **Rapor yaz** — `deliverables/FX2-KAPSAM-RAPORU.md`:
   - **Öncesi/sonrası kapsam tablosu** (M1–M9, taban → şimdi → eşik → ✅/❌)
   - Dalga bazında ne uygulandı
   - Fixture envanteri ve sonuçları
   - Değişmezlik kanıtları
   - **Kanıt seviyesi ayrımı** (`KAPSAM-OLCUMU.md` §6): K1 kaynak/test var,
     K2 yerel görsel varsa, **K3 cihaz kabulü kullanıcıdan bekleniyor**
   - Bilinen sınırlar: iOS haptik yok, sessiz anahtar, bulut TTS anahtar bağımlı

## Kabul kriterleri
- [ ] Tüm aileler **0 FAIL**
- [ ] `--gate` PASS
- [ ] `App.*` ve `onclick` sayıları kartların beklediği değerlerde
- [ ] Rapor K1/K2/K3 ayrımını açıkça yapıyor
- [ ] **Hiçbir kaynak dosya değişmedi** (`git status --short` yalnız
      `deliverables/`)

## Geri alma
Üretilen rapor dosyasını sil: `premium-fx-plan/deliverables/FX2-KAPSAM-RAPORU.md`

## Kayıt
Commit: `fx2: FX2-P-52 tam regresyon ve kapsam raporu`
