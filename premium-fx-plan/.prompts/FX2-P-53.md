# FX2-P-53 — Kapanış ve Doküman Senkronu

**Dalga:** 5 · **Tip:** belge · **Hedef:** tek doğru kaynak

## Adımlar

1. **`deliverables/FX2-KAPANIS.md` yaz:**
   - Seri özeti (20 kart, 6 dalga, yerel commit sayısı)
   - **Öncesi/sonrası kapsam tablosu** (asıl kanıt — FX-1'in kapanışında
     olmayan şey)
   - Nihai API envanteri: `SeyTouch`, `SeyAudio` (11+ ses), `SeyFx`
     (+`sheetClose`, `sweepCounters`, `bindAuroraParallax`), `SeyHaptics`,
     `SeyTimeTheme`
   - Yeni/değişen `settings` alanları ve varsayılanları
   - **Bilinen sınırlar** (dürüstçe): iOS haptik yok; iOS sessiz anahtarı
     WebAudio'yu susturabilir; bulut TTS anahtar gerektirir;
     `render()` hâlâ tam `innerHTML` — sürekli hareket sınırlı
   - Push/merge/deploy durumu: **yapılmadı, kullanıcı onayı bekliyor**

2. **`README.md` güncelle:** durum tablosu, sıradaki iş, kapsam özeti.

3. **`.anti-amnesia/` finalize:** `FX2-STATE.json`
   (`implementationComplete:true`, `pushedToRemote:false`,
   `blockedPrompt:null`), `CURRENT-STATE.md` son bölüm, `LEDGER.md` son satır.

4. **Kök yönlendirmeleri güncelle** — `CLAUDE.md` ve `AGENTS.md` premium-FX
   maddesi:
   - Giriş noktası `premium-fx-plan/TESHIS.md` → `PLAN-FX2.md`
   - `MODULARIZATION.md` referansı **korunur**
     (`tests/app/test_modularization_boundary.js` ona bağlı)
   - Silinen belgelere (PLAN.md, ROADMAP.md, NEXT-STEPS.md, CODE-MAP.md,
     FX-LIBRARY.md, API-TRANSITION-GUIDE.md, DEEP-IMPLEMENTATION-GUIDE.md,
     SPEC-FAZ-*.md) yapılan atıflar temizlenir
   - Repo düzeni bölümündeki `premium-fx-plan/` açıklaması yenilenir

5. **`docs/GELISTIRME-PLANI.md`** durum tablosuna premium FX satırı eklenir/
   güncellenir.

6. **Ölü bağlantı taraması:**
   ```bash
   grep -rn 'premium-fx-plan/' --include='*.md' . | grep -v '^./premium-fx-plan/.prompts/' \
     | grep -oE 'premium-fx-plan/[A-Za-z0-9_./-]+' | sort -u \
     | while read p; do [ -e "$p" ] || echo "ÖLÜ: $p"; done
   ```
   Çıktı **boş** olmalı.

## Kabul kriterleri
- [ ] `FX2-KAPANIS.md` kapsam tablosu içeriyor
- [ ] `CLAUDE.md` + `AGENTS.md` güncel, ölü bağlantı yok
- [ ] `MODULARIZATION.md` dokunulmadı; `test_modularization_boundary.js` yeşil
- [ ] `.anti-amnesia/` üç dosya tutarlı
- [ ] `monolit-bolumlenme-plan/` referansları hâlâ çözülüyor
- [ ] **Push/merge/deploy yapılmadı**

## Geri alma
Değişen belge dosyalarını git ile eski hâline getir.

## Kayıt
Commit: `fx2: FX2-P-53 kapanış ve doküman senkronu`
